- test:
    name: 临时车严进-不需缴费严出
    desc: 临时车严进,不需缴费严出
    send_data:
      parkId: ${FreePark_parkID}
      parkUUID: ${FreePark_parkUUID}
      carNum: ${__create_carNum()}
      inClientID: ${freePark_StrictRule_inClientID}
      outClientID: ${freePark_StrictRule_outClientID}
      carIn_jobId: ${临时车严进-不需缴费严出.carIn_jobId}
      carOut_jobId: ${临时车严进-不需缴费严出.carOut_jobId}
    except:
      mockCarInMessage: 临时车请稍候
      checkCarInMessage: true
      checkCarInVoice: $${__create_carNum()}$登记放行
      checkCarInScreen: ${__create_carNum()}\\登记放行
      checkCarInIsOpenGate: 1
      presentCarMessage: ${__create_carNum()}
      parkingBillDetailMessage: ${__create_carNum()}
      mockCarOutMessage: 临时车请稍候
      checkCarOutMessage: true
      checkCarOutVoice: $${__create_carNum()}$一路顺风
      checkCarOutScreen: ${__create_carNum()}\\一路顺风
      checkCarOutIsOpenGate: 1
      carLeaveHistoryMessage: ${__create_carNum()}

