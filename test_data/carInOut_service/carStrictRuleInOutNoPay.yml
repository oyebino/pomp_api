- test:
    name: 临时车严进-不需缴费严出
    desc: 临时车严进,不需缴费严出
    send_data:
      parkId: ${FreePark_parkID}
      carNum: ${__create_carNum()}
      inClientID: ${freePark_StrictRule_inClientID}
      outClientID: ${freePark_StrictRule_outClientID}
      carInJobId: ${临时车严进-不需缴费严出.carIn_jobId}
      carOutJobId: ${临时车严进-不需缴费严出.carOut_jobId}
    except:
      mockCarInMessage: 临时车请稍候
      presentCarMessage: ${__create_carNum()}
      isOpenGate: 1
      parkingBillDetailMessage: ${__create_carNum()}
      mockCarOutMessage: 临时车请稍候
      carLeaveHistoryMessage: ${__create_carNum()}

- test:
    name: 新能源小车严进-不需缴费严出
    desc: 新能源小车严进，不需缴费严出
    send_data:
      parkId: ${FreePark_parkID}
      carNum: ${__create_carNum(carType=新能源)}
      inClientID: ${freePark_StrictRule_inClientID}
      outClientID: ${freePark_StrictRule_outClientID}
      carInJobId: ${新能源小车严进-不需缴费严出.carIn_jobId}
      carOutJobId: ${新能源小车严进-不需缴费严出.carOut_jobId}
    except:
      mockCarInMessage: 临时车请稍候
      presentCarMessage: ${__create_carNum(carType=新能源)}
      isOpenGate: 1
      parkingBillDetailMessage: ${__create_carNum(carType=新能源)}
      mockCarOutMessage: 临时车请稍候
      carLeaveHistoryMessage: ${__create_carNum(carType=新能源)}